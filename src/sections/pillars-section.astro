---
import { CircleChevronRight, MoveRight } from 'lucide-react';

// import chronogram from "@/assets/pilares-mockup.svg";
import MotionSection from '@/components/ui/MotionSection';

const firstPart = ['Análise detalhada de seu curriculo e linkedin', 'Seu portfolio', 'Seu histórico profissional', 'Sua rotina de estudos'];

const secondPart = [
  'Seus gaps técnicos e comportamentais',
  'Seus objetivos de carreira',
  'Sua carta apresentação/ cover letter',
  'Seu posicionamento e conhecimento em entrevistas técnicas e comportamentais',
  'Seu personal branding',
  'Como o mercado te vê hoje',
];
---

<MotionSection client:visible>
  <section id='pillars-section' class='px-6 py-20'>
    <div class='flex flex-col items-center justify-center max-w-6xl gap-16 mx-auto text-center'>
      <h2 class='max-w-3xl heading-section'>
        Você vai dominar a estratégia que me fez me tornar sênior com 5 anos no mercado de trabalho
      </h2>

      <!-- <Image 
        src={chronogram} 
        alt="Cronograma dos pilares"
        class="object-contain w-full h-auto max-w-4xl mx-auto shadow-lg rounded-xl"
      /> -->
      <!-- <img
        src='/assets/pilares.png'
        alt='Cronograma dos pilares'
        class='hidden object-contain w-full h-auto max-w-4xl mx-auto shadow-lg rounded-xl md:block'
      /> -->
      <div>
        <h3 class='max-w-3xl subheading-section text-zinc-200'>
          Aplicando os 3 pilares base do meu método de aceleração
          <strong>Posicionamento Estratégico</strong>,
          <strong>Soft Skills</strong>, e <strong>Hard Skills</strong>.
        </h3>
      </div>

      <div class='grid grid-cols-1 md:grid-cols-[1fr_auto_1fr] gap-8 md:gap-12 items-center'>
        <div
          class='flex flex-col w-full h-full max-w-sm p-6 border shadow-lg border-zinc-300 rounded-2xl bg-gradient-to-b from-zinc-100 via-zinc-200 to-zinc-300'
        >
          <h3 class='mb-4 text-lg font-extrabold tracking-tight uppercase text-zinc-700'>
            O que está incluso na consultoria ?
          </h3>
          <ul role='list' class='flex flex-col items-start gap-4 text-zinc-700 body-text'>
            {
              firstPart.map((item) => (
                <li role='listitem' class='flex items-center gap-3 font-medium text-left'>
                  <CircleChevronRight className='w-5 h-5 text-emerald-600 shrink-0' />
                  <span>{item}</span>
                </li>
              ))
            }
          </ul>
        </div>

        <MoveRight
          className='self-center hidden w-12 h-12 md:block text-muted-foreground shrink-0'
          aria-hidden='true'
        />

        <div
          class='flex flex-col w-full h-full max-w-sm p-6 border shadow-lg border-zinc-300 rounded-2xl bg-gradient-to-b from-zinc-100 via-zinc-200 to-zinc-300'
        >
          <h3 class='mb-4 text-lg font-extrabold tracking-tight uppercase text-zinc-700'>
           E tem mais:
          </h3>
          <ul role='list' class='flex flex-col items-start gap-4 text-zinc-700 body-text'>
            {
              secondPart.map((item) => (
                <li role='listitem' class='flex items-center gap-3 font-medium text-left'>
                  <CircleChevronRight className='w-5 h-5 text-emerald-600 shrink-0' />
                  <span>{item}</span>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </section>
</MotionSection>

